import { Spin } from 'antd'
import React from 'react'

const Loader = (props) => {
    return (
        <div style={{color:"black",textAlign:"center"}}>
            {/* <Spin spinning={props.enable} style={{padding:"20rem"}} tip="Loading..." size="large" delay={5} >
            </Spin> */}
            
        </div>
    )
}

export default Loader
